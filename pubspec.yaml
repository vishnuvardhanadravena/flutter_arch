name: flutter_arch
description: A CLI tool to generate Flutter project structure with clean architecture and best practices.
version: 1.1.1
repository: https://github.com/your_org/flutter_arch

environment:
  sdk: ^3.12.1

dependencies:
  args: ^2.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  flutter_arch: flutter_arch

